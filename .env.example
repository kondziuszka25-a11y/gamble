# Frontend Environment Variables (Vite)
# Leave empty if frontend is served by the same Express server in production or in local dev (uses Vite proxy).
# If frontend is deployed separately (e.g. Vercel / Netlify), set this to your backend domain:
# VITE_API_URL=https://api.yourdomain.com
VITE_API_URL=
